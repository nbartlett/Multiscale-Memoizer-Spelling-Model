Global setup:
 Download and install Git
  git config --global user.name "Your Name"
  git config --global user.email bartlett@stat.columbia.edu
        
Next steps:
  mkdir Multiscale-Memoizer-Spelling-Model
  cd Multiscale-Memoizer-Spelling-Model
  git init
  touch README
  git add README
  git commit -m 'first commit'
  git remote add origin git@github.com:nbartlett/Multiscale-Memoizer-Spelling-Model.git
  git push -u origin master
      
Existing Git Repo?
  cd existing_git_repo
  git remote add origin git@github.com:nbartlett/Multiscale-Memoizer-Spelling-Model.git
  git push -u origin master